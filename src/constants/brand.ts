/**
 * The product's own name and wordmark letter, as the NYX prototype states them
 * (prototype source lines 2262, 2276, 193). It appears on the
 * sign-in brand panel, the signing-in bar and the application top bar, so it
 * lives here rather than being retyped at three call sites.
 *
 * `BRAND_LETTER` is the prototype's logo placeholder — a white tile carrying a
 * teal "L". It is a stand-in for an Oman LNG mark that the engagement has not
 * delivered; when a real logo asset lands it replaces `BrandMark`'s body, and
 * this constant goes with it.
 */
export const BRAND_NAME = "AI E-Logbook Platform";

export const BRAND_LETTER = "L";
